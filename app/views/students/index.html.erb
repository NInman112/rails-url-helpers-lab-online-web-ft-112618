<div>
  <% @students.each do |student| %>
  <div><%= link_to student.to_s, "/student/#{student.id}" %></div>
  <div><%= link_to student.to_s, post_path(student.id) %></div>
    <div><%= link_to student.to_s, student_path(student) %></div>
  <% end %>
</div>
